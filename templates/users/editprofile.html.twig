{% extends 'users.html.twig' %}

{% block title %}Moficication du Profil de {{ app.user.name }} {{ app.user.firstname }}
{% endblock %}

{% block body %}

	<div class="grix xs4 container">
        <div class="col-xs4">
            <h1>Profil de {{ app.user.name }} {{ app.user.firstname }}</h1>
            {{ form(form) }}
        </div>
		
	</div>

{% endblock %}
